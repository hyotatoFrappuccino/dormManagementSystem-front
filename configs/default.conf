server {
    listen 80;
    location / {
        # 서버의 루트 디렉토리
        root /usr/share/nginx/html;
        # 기본적으로 찾아볼 파일을 순서대로 지정함
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}